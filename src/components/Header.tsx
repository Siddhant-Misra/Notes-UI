import React from "react";
import "../styles/Header.css";

const Header: React.FC = () => {
  return (
    <header className="header">
      <h4>Generic Notes App</h4>
    </header>
  );
};

export default Header;
